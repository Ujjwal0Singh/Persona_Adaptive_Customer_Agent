# Copy this file to .streamlit/secrets.toml for local Streamlit-secrets testing,
# OR paste its contents into the "Secrets" box when deploying on
# Streamlit Community Cloud (App settings > Secrets).
#
# This example file itself is safe to commit - it contains no real key.

GEMINI_API_KEY = "Your_api_key"